from setuptools import setup, find_packages

setup(
    name="LogAnalytics",
    version="0.0.1",
    description="Microsoft Azure Log Analytics SDK for Python 3.6",
    author="sebastian",
    author_email="seba@cloudnative.co.jp",
    packages=find_packages(),
    install_requires=[
    ],
    entry_points={
        "console_scripts": [
        ]
    },
)
